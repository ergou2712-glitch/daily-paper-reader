---
title: "Can We Trust LLM's Logic? Quantifying Uncertainty, Coherence, and Robustness via a Graph-Based Framework"
title_zh: 我们能信任大语言模型的逻辑吗？通过基于图的框架量化不确定性、连贯性与鲁棒性
authors: "Riccardo Revalor, Jalees Rehman, Debjit Pal"
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08017v1"
tags: ["query:q4"]
score: 8.0
evidence: 量化大语言模型中的不确定性与推理忠实度
tldr: 针对大语言模型推理逻辑不透明及现有自一致性方法仅关注最终答案的局限，本文提出GRAPHEVAL框架。该框架通过构建推理图，引入GRCS指标量化语义与结构的共识，并提出GSC解码策略。实验证明GRCS能有效识别虚假推理，而GSC在提升推理忠实度的同时，揭示了模型推理的鲁棒性与关键逻辑路径。
selection_source: fresh_fetch
motivation: 现有的自一致性方法仅评估最终答案的一致性而忽略了中间推理步骤的逻辑正确性，导致难以准确量化推理的不确定性。
method: 提出基于图的推理框架GRAPHEVAL，引入量化语义结构共识的GRCS指标以及基于中心点的GSC解码策略。
result: GRCS指标与推理失信度呈显著负相关，且GSC策略在提升推理忠实度的同时，能有效识别并减少模型中的“侥幸猜对”现象。
conclusion: 通过图论方法分析推理拓扑，可以更可靠地量化LLM的推理不确定性，并识别出支撑逻辑正确性的关键路径。
---

## 摘要
大语言模型（LLMs）容易产生错误且不忠实的推理，而像自一致性（Self-Consistency, SC）这样的解码策略无法检测到这些问题，因为它们仅评估最终答案的一致性，而忽略了中间步骤的逻辑有效性。这引出了三个基本问题：我们如何可靠地量化 LLM 推理中的不确定性？与朴素的多数投票相比，语义、结构和因果感知能否选择更忠实的推理？以及推理拓扑在对抗条件下有多稳健？为了解决这些问题，我们引入了 GRAPHEVAL，这是一个基于图的推理框架，它将不确定性量化（UQ）重新定义为一个整体的推理保真度问题。我们提出了一种新型的 UQ 指标——图推理连贯性得分（Graph Reasoning Coherence Score, GRCS），它量化了推理空间的语义-结构共识，并能捕捉病态的模式崩溃和自信的幻觉。我们发现，在能力较强和较小的模型中，GRCS 是唯一与推理忠实度始终保持负相关的指标。此外，我们引入了图自一致性（Graph Self-Consistency, GSC），这是一种基于中心点的解码策略，它以名义准确率换取推理保真度，揭示了在较小模型中 SC 因不忠实的“幸运猜测”而虚高的程度，同时在能力较强的模型中保持或提高了准确率。最后，通过对抗性中心点消融，我们证明了 GSC 选择的路径起到了“承重路径”的作用，迫使模型偏离该路径会降低推理忠实度，并在特定情况下导致准确率下降。

## Abstract
Large-Language Models (LLMs) can be prone to flawed and unfaithful reasoning that decoding strategies like Self-Consistency (SC) fail to detect as they evaluate only final-answer agreement while ignoring the logical validity of intermediate steps. This raises three fundamental questions: How can we reliably quantify uncertainty in LLM reasoning? Can semantic, structural, and causal awareness select more faithful reasoning compared to naïve majority voting? and How robust is reasoning topology under adversarial conditions? To address these questions, we introduce GRAPHEVAL, a graph-based reasoning framework that re-frames uncertainty quantification (UQ) as a holistic reasoning fidelity problem. We propose a novel UQ metric, Graph Reasoning Coherence Score (GRCS), that quantifies semantic-structural consensus of the reasoning space and captures pathological mode collapse and confident hallucinations. We find that GRCS is the only metric that is consistently negatively correlated with reasoning faithfulness across both more capable and smaller models. Additionally, we introduce Graph Self-Consistency (GSC), a medoid-based decoding strategy that trades nominal accuracy for reasoning fidelity, exposing the degree to which SC is inflated by unfaithful lucky guesses in smaller models, while preserving or improving accuracy in more capable ones. Finally, through adversarial medoid ablation, we demonstrate that the GSC-selected path acts as a "load-bearing path" and forcing models away from it degrades reasoning faithfulness and, in targeted cases, causes drops in accuracy.

---

## 论文详细总结（自动生成）

### 论文总结：我们能信任大语言模型的逻辑吗？通过基于图的框架量化不确定性、连贯性与鲁棒性

#### 1. 核心问题与整体含义
论文探讨了大语言模型（LLM）在复杂推理任务中的**信任危机**。核心问题在于：LLM 经常通过错误的推理步骤意外得到正确答案（即“幸运猜测”或“虚假推理”）。现有的主流解码策略如“自一致性”（Self-Consistency, SC）仅通过最终答案的多数投票来决定输出，完全忽略了中间推理逻辑的正确性。这导致在医疗、法律等关键领域，模型给出的答案虽然看似正确，其背后的逻辑可能漏洞百出。

#### 2. 方法论：GRAPH EVAL 框架
论文提出了一个基于图论的推理评估框架，将推理过程从线性文本转化为**有向无环图（DAG）**。
*   **因果事实分解与构图**：使用一个专门的分解模型（DLLM，如 GPT-4o）将生成模型（GLLM）产生的推理链拆解为原子事实（主-谓-宾结构），并根据声明的因果关系构建 DAG。
*   **SS-GED（结构-语义图编辑距离）**：提出一种改进的图距离算法。它不仅计算拓扑结构的差异（通过匈牙利算法优化，复杂度 $O(m^3)$），还结合了节点和终端答案的语义相似度（余弦距离）。
*   **GRCS（图推理连贯性得分）**：一种新型不确定性量化（UQ）指标。通过计算推理流形中所有路径的平均成对距离，量化推理空间的共识程度。
*   **GSC（图自一致性）解码**：不再简单投票，而是选择推理空间中的**中心点（Medoid）**路径。该路径在结构上最居中，且在语义上获得最多支持。
*   **对抗性中心点消融**：通过提示词强制模型避开 GSC 选出的核心路径，观察其推理能力的退化情况。

#### 3. 实验设计
*   **数据集**：涵盖了不同认知难度的任务：
    *   GSM8K（小学数学）
    *   BoolQ（是非阅读理解）
    *   StrategyQA（常识推理）
    *   MedQA USMLE（医学执照考试）
    *   GPQA Diamond（研究生水平科学问题）
*   **对比模型（GLLM）**：Llama 3.1 8B、Phi-4 (14B)、DeepSeek R1 (Distill-Llama-70B)。
*   **Benchmark 与对比方法**：
    *   **UQ 指标对比**：CoTA、Topo-UQ、离散度（Dispersion）、熵（Entropy）。
    *   **解码策略对比**：自一致性（SC）。
    *   **忠实度验证**：采用“提前回答”（Early Answering）协议，测试模型是否在推理未完成时就已“偷看”答案。

#### 4. 资源与算力
*   论文**未明确说明**具体的硬件型号（如 GPU 数量）或总训练/推理时长。
*   文中提到使用了 OpenAI 的 **GPT-4o** 作为分解器（DLLM），**Qwen3-Embedding-0.6B** 作为嵌入模型。
*   实验涉及对每个问题采样 $N=20$ 条推理路径，这在推理阶段具有一定的计算开销。

#### 5. 实验数量与充分性
*   **实验规模**：在 5 个数据集上对 3 种不同架构和规模的模型进行了全面测试。
*   **消融实验**：详细测试了 DLLM 的选择对结果的影响、SS-GED 算法中超参数（$\alpha, \beta, \gamma$）的敏感性。
*   **客观性**：通过人类评估者与 LLM-as-a-judge（Gemini 3 Pro）的双重审计，计算了 Cohen’s $\kappa$ 系数（0.913），证明了评估的可靠性。
*   **充分性**：实验不仅关注准确率，还深入分析了“虚假推理”的占比，并引入对抗性攻击来验证逻辑路径的必要性，实验逻辑闭环且严谨。

#### 6. 主要结论与发现
*   **GRCS 是最稳健的指标**：在 93% 的配置中，GRCS 与推理忠实度呈显著负相关，能有效识别出那些“自信的幻觉”。
*   **小模型存在严重“水分”**：在 Llama 3.1 8B 等小模型中，SC 获得的准确率很大程度上依赖于“幸运猜测”。使用 GSC 解码后，虽然名义准确率下降，但推理忠实度大幅提升。
*   **大模型更具鲁棒性**：对于 DeepSeek R1 等强模型，GSC 不仅能保持准确率，甚至在某些任务上有所提升，说明其推理中心路径更趋向于正确逻辑。
*   **承重路径发现**：对抗性实验证明，GSC 选出的中心路径是模型的“承重路径”，一旦被禁用，模型往往无法通过其他有效逻辑找回正确答案。

#### 7. 优点
*   **视角独特**：将 LLM 推理评估从“黑盒结果”转向“白盒拓扑”，填补了推理过程量化的空白。
*   **算法优化**：通过匈牙利算法将原本 NP-hard 的图编辑距离计算降阶为多项式复杂度，使其具备实际应用价值。
*   **深度洞察**：揭示了模型规模与推理多样性、忠实度之间的复杂关系。

#### 8. 不足与局限
*   **计算成本**：相比于简单的 SC 投票，构建图并计算成对距离的开销较大（$O(N^2 \cdot m^3)$）。
*   **模型依赖**：框架的效果部分取决于分解模型（DLLM）的质量，若 DLLM 无法准确提取因果关系，图的质量会受损。
*   **任务范围**：目前主要集中在短程推理（3-6 步），对于需要极长链条推理的任务，图的复杂度和噪声可能会显著增加。
*   **实时性**：由于需要多次采样和复杂的图计算，该方法目前更适合离线评估或高价值决策场景，而非实时对话。

（完）
