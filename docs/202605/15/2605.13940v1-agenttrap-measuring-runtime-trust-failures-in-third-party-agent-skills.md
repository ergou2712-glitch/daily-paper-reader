---
title: "AgentTrap: Measuring Runtime Trust Failures in Third-Party Agent Skills"
title_zh: AgentTrap：衡量第三方智能体技能中的运行时信任失效
authors: "Haomin Zhuang, Hanwen Xing, Yujun Zhou, Yuchen Ma, Yue Huang, Yili Shen, Yufei Han, Xiangliang Zhang"
date: 2026-05-13
pdf: "https://arxiv.org/pdf/2605.13940v1"
tags: ["query:q3"]
score: 9.0
evidence: 衡量运行时信任失效的基准测试
tldr: 本研究针对大语言模型智能体第三方技能生态中的安全风险，提出了动态基准测试框架 AgentTrap。该框架包含 141 个任务，涵盖 16 个安全维度，旨在评估智能体在执行看似正常的任务时，能否识别并抵御隐藏在第三方技能中的恶意运行时行为。研究发现，智能体往往会将恶意副作用视为正常工作流的一部分，而非触发传统的安全拒绝，这为智能体安全评估提供了新视角。
selection_source: fresh_fetch
motivation: 第三方技能可能将恶意行为伪装成常规工作流，利用智能体的高权限和低监管实施攻击，现有的静态评估难以捕捉这种运行时信任失效。
method: 开发了包含 91 个恶意和 50 个良性任务的 AgentTrap 基准，在沙箱环境中通过分析完整执行轨迹来评估智能体的防御能力。
result: 实验发现模型常在完成用户任务的同时，将技能引入的有害副作用误认为是正常流程的一部分并予以执行。
conclusion: 评估 LLM 智能体安全必须深入到模型、框架与工作空间集成的实际运行环境中，而不能仅依赖于孤立的模型越狱测试。
---

## 摘要
第三方技能正成为大语言模型（LLM）智能体的包生态系统。它们将自然语言指令、辅助脚本、模板、文档和服务配置封装进可重用的工作流中。这使得技能非常实用，但也引入了新的安全问题：恶意技能无需请求模型执行明显有害的操作。相反，它可以将有害行为伪装成常规工作流的一部分，依靠智能体在拥有高价值权限且人类监督有限的情况下执行该工作流。我们推出了 AgentTrap，这是一个动态基准测试，用于评估 LLM 智能体在利用第三方技能时能否抵御恶意的运行时行为。AgentTrap 包含 141 个任务：91 个恶意任务和 50 个良性实用任务，涵盖了基于智能体技能供应链威胁的 16 个安全影响维度。在每个任务中，智能体接收普通的用户请求，运行已安装的可能包含恶意工作流元素的技能，并在沙箱环境中执行。AgentTrap 随后对完整的轨迹进行评判，包括攻击成功、行为被拦截或拒绝、攻击未触发以及无攻击证据等结果。我们的核心发现是，最具参考价值的失效并非简单的越狱。模型通常在完成可见的用户任务的同时，将技能引入的不安全副作用视为正常工作流的一部分。这促使我们需要对用户实际委托工作的具体“模型-框架-工作空间”环境进行运行时评估。代码和数据可在 https://github.com/zhmzm/AgentTrap 和 https://huggingface.co/datasets/zhmzm/AgentTrap 获取。

## Abstract
Third-party skills are becoming the package ecosystem for LLM agents. They package natural-language instructions, helper scripts, templates, documents, and service configuration into reusable workflows. This makes skills useful, but it also introduces a new security problem: a malicious skill does not need to ask the model to perform an obviously harmful action. Instead, it can disguise the harmful behavior as part of a routine workflow, relying on the agent to execute that workflow with high-value permissions and limited human supervision.   We introduce AgentTrap, a dynamic benchmark for evaluating whether LLM agents can use third-party skills while resisting malicious runtime behavior. AgentTrap contains 141 tasks: 91 malicious tasks and 50 benign utility tasks, covering 16 security-impact dimensions grounded in agent-skill supply-chain threats. In each task, the agent receives an ordinary user request, runs with installed skills that may contain malicious workflow elements, and is executed in a sandboxed environment. AgentTrap then judges complete trajectories for attack success, blocked or refused behavior, attack-not-triggered cases, and no-attack-evidence outcomes. Our central finding is that the most informative failures are not simple jailbreaks. Models often complete the visible user task while treating unsafe side effects introduced by the skill as part of the normal workflow. This motivates runtime evaluation of the concrete model--framework--workspace environment in which users actually delegate work. Code and data are available at https://github.com/zhmzm/AgentTrap and https://huggingface.co/datasets/zhmzm/AgentTrap.

---

## 论文详细总结（自动生成）

### 论文总结：AgentTrap —— 衡量第三方智能体技能中的运行时信任失效

#### 1. 核心问题与研究动机
随着大语言模型（LLM）智能体（Agents）的普及，第三方“技能”（Skills，包含自然语言指令、脚本、模板和配置的包）成为了智能体生态的核心。然而，这引入了严重的**运行时信任边界问题**：
*   **伪装性强**：恶意技能不需要诱导模型进行明显的有害操作（如传统越狱），而是将攻击行为伪装成常规工作流的一部分。
*   **高权限风险**：智能体通常拥有文件系统、Shell、API 凭据等高价值权限，且人类监督有限。
*   **现有评估不足**：现有的基准测试多关注静态包扫描或固定环境下的越狱，忽略了动态运行环境中模型、框架与工作空间交互产生的安全漏洞。

#### 2. 方法论
论文提出了 **AgentTrap**，一个用于评估智能体在实际运行中抵御恶意第三方技能能力的动态基准测试框架。
*   **核心思想**：将攻击载荷嵌入到看似正常的技能工作流中，观察智能体在执行普通用户请求时，是否会触发并执行这些隐藏的恶意行为。
*   **16 个安全影响维度**：涵盖数据外泄、系统完整性破坏、配置投毒、资源滥用、代码注入、权限提升等。
*   **10 种运行时攻击方法**：包括技能指令投毒、辅助脚本副作用、隐写术载荷、隐藏路由、MCP（模型上下文协议）滥用等。
*   **双路径执行评估**：
    *   **纯智能体路径（Plain Agent）**：仅提供最小化的工具调用接口，测试模型自身的安全意识。
    *   **框架介导路径（Framework-mediated）**：将模型嵌入实际的智能体框架（如 Claude Code, OpenClaw），测试框架层面的防御与环境配置的影响。
*   **评判机制**：基于沙箱环境中的完整执行轨迹（Trajectory），结合确定性检查（如文件变更、网络请求）和 LLM 评判器（GPT-4o-mini）进行分类标注。

#### 3. 实验设计
*   **数据集/场景**：包含 141 个可执行任务，其中 91 个恶意任务（基于真实世界 ClawHub 等平台的技能改造），50 个良性实用任务（用于检测过度防御）。
*   **任务类别**：涵盖文档处理、搜索研究、自动化集成、软件开发、通信协作等 7 大类。
*   **对比方法**：
    *   **静态扫描器**：对比了 Cisco 静态分析、VirusTotal、LLM 语义扫描等。
    *   **主流模型**：测试了 Claude 3.5 Sonnet/Haiku、GPT-4o/4o-mini、Qwen2.5-Coder、GLM-4、Kimi-k1.5 等。
    *   **智能体框架**：对比了原生 API 调用与集成框架（如 Claude Code）的表现。

#### 4. 资源与算力
*   论文**未明确说明**具体的 GPU 型号、数量或总训练时长。
*   由于该研究属于基准测试和推理评估，主要算力消耗在于运行 LLM API 进行任务执行和结果评判，而非模型训练。

#### 5. 实验数量与充分性
*   **实验规模**：进行了 141 个任务在多个模型和框架组合下的完整运行，产生了大量的执行轨迹数据。
*   **充分性**：实验覆盖了从模型层到框架层再到用户环境层（如加载特定的 `.md` 记忆文件）的多个维度，能够较好地揭示安全风险的来源。
*   **客观性与公平性**：所有任务在标准化的沙箱环境中运行，使用预定义的成功信号（Success Signal）进行评判，避免了仅凭主观判断。同时引入良性任务来平衡评估，防止模型通过“一味拒绝”来刷高安全分。

#### 6. 主要结论与发现
*   **隐蔽失效是主流**：最常见的失效不是模型被“越狱”，而是模型在完成用户任务的同时，顺从地执行了技能中不安全的副作用，将其视为正常流程。
*   **静态防御局限性**：传统杀毒软件（如 VirusTotal）对这类基于语义和行为的攻击检出率为 0，即使是 LLM 静态扫描也会漏掉大量运行时风险。
*   **框架的双刃剑效应**：强大的智能体框架（如 Claude Code）虽然能提供更多防御，但由于其更强的执行能力，有时反而会触及更多恶意载荷，导致攻击成功率上升。
*   **环境敏感性**：用户的本地指令文件（如 `CLAUDE.md`）和记忆会显著改变智能体的安全边界。

#### 7. 优点（亮点）
*   **真实性高**：任务源于真实的技能生态系统，而非纯人工构造的对抗性提示词。
*   **多模态攻击面**：不仅关注文本指令，还涵盖了脚本代码、生成的中间产物、标准输出（stdout）等多种攻击媒介。
*   **诊断性强**：通过双路径设计，能够区分安全失效是源于模型本身、框架逻辑还是环境配置。

#### 8. 不足与局限
*   **诊断而非因果**：虽然能观察到失效，但难以完全解耦模型、框架与环境之间的复杂因果关系。
*   **覆盖范围偏差**：为了保证 16 个维度的平衡，任务分布可能与现实市场中恶意技能的实际流行频率存在偏差。
*   **评判器依赖**：部分语义复杂的任务仍依赖 LLM 作为评判器，可能存在潜在的评判偏差。
*   **应用限制**：目前主要针对类 Unix 的沙箱环境，对其他操作系统或特定企业级环境的覆盖有限。

（完）
