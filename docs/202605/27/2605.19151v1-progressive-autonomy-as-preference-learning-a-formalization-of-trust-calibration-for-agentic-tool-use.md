---
title: "Progressive Autonomy as Preference Learning: A Formalization of Trust Calibration for Agentic Tool Use"
authors: Changkun Ou
date: 2026-05-18
pdf: "https://arxiv.org/pdf/2605.19151v1"
tags: ["query:q3"]
score: 9.0
evidence: 代理工具使用的信任校准形式化
tldr: 将信任校准形式化为偏好学习问题，以决定智能体何时需要人工批准。
selection_source: fresh_fetch
motivation: 代理工具使用的信任校准形式化。
method: 方法与实现细节请参考摘要与正文。
result: 结果与对比结论请参考摘要与正文。
conclusion: 总体而言，该工作在所述任务上展示了有效性，并提供了可复用的思路或工具。
---

## Abstract
We formalize trust calibration for agentic tool use (deciding when an automated agent's proposed action may execute autonomously versus require human approval) as a preference-learning problem. A policy gateway maintains a Gaussian-process posterior over a latent human risk-tolerance function, observed through a probit likelihood on binary approve/deny feedback, and escalates to the human exactly where the approval outcome is most uncertain. We show this is structurally an instance of Preferential Bayesian Optimization, inheriting its inference machinery (approximate Gaussian-process classification) and its sample-efficiency argument (uncertainty-targeted querying), while differing in objective: classifying an action space into allow/block/ask regions rather than optimizing a design.

---

## 论文详细总结（自动生成）

这篇论文《Progressive Autonomy as Preference Learning: A Formalization of Trust Calibration for Agentic Tool Use》由 Changkun Ou 撰写，旨在为智能体（Agent）工具使用的“信任校准”提供一种形式化的数学框架。

以下是对该论文的结构化总结：

### 1. 核心问题与研究动机
*   **核心问题**：如何科学地决定一个自动化智能体在执行任务时，何时可以自主运行，何时必须请求人工批准（即“升级”到人工干预）。
*   **研究动机**：目前的智能体治理大多停留在定性描述或硬编码的规则阶段。作者认为，自主权的授予不应是静态的阈值，而应是一个动态学习的过程。通过将“信任校准”形式化为**偏好学习（Preference Learning）**问题，使系统能够从人类的二元反馈（批准/拒绝）中自动学习风险偏好，实现渐进式自主。

### 2. 方法论
*   **核心思想**：将人类的风险容忍度建模为一个潜在函数 $f$，并利用**高斯过程（Gaussian Process, GP）**进行后验估计。
*   **关键技术细节**：
    *   **潜在风险函数**：假设存在函数 $f(x)$，其中 $x$ 包含动作（工具、参数）和上下文（代码库状态、任务描述）。
    *   **观测模型**：采用 Probit 似然模型，将人类的批准概率表示为标准正态分布的累积分布函数 $\Phi(f(x))$。这与偏好贝叶斯优化（PBO）的结构一致。
    *   **结构化核函数**：设计了一个乘积核，分别捕捉工具相似性（k_tool）、上下文相似性（k_ctx）和时间非平稳性（k_time）。
    *   **三级决策网关**：
        1.  **Allow（允许）**：预测批准概率高于高阈值 $\tau_{high}$。
        2.  **Block（拦截）**：预测批准概率低于低阈值 $\tau_{low}$。
        3.  **Ask（询问）**：处于中间不确定区域，触发人工干预。
    *   **非平稳性处理**：引入时间衰减核，使模型能赋予近期反馈更高权重，模拟人类信任随时间漂移（如从谨慎到信任）的过程。

### 3. 实验设计
*   **实验场景**：由于缺乏现成的、带有长期个人偏好漂移记录的公开数据集，作者设计了一个**受控仿真研究**。
*   **仿真设置**：包含 18 种工具、8 个资源敏感度等级和 7 个任务上下文。模拟了一个具有“信任漂移”和“突发变更点（Changepoint）”的人类监督者。
*   **基准方法（Benchmark）**：
    *   **Independent Baseline**：不考虑核相关性，独立学习每个工具的上下文强盗模型。
    *   **Always-Escalate**：现状基准，即所有动作均需人工审批。
    *   **Random Querying**：用于评估主动学习效率的对比项。
*   **评估指标**：自动决策准确率、误报率（False-allow）、人工负担减轻比例、RMSE 和 ECE（校准误差）。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的 GPU 型号或训练时长。
*   **计算复杂度**：作者指出，在单个项目的决策规模下（$N$ 在 $10^2$ 到 $10^3$ 数量级），使用 Laplace 近似的高斯过程推理计算开销极低，不需要稀疏近似，可以在普通硬件上实时运行。

### 5. 实验数量与充分性
*   **实验规模**：进行了 1500 个决策点的流式实验，重复 6 个随机种子。
*   **消融实验**：针对“采集规则（Acquisition Rule）”进行了消融，对比了“询问带（Ask-band）”策略与随机查询的效果。
*   **充分性评价**：实验在验证核函数的泛化能力、处理非平稳性以及减轻人工负担方面是充分且客观的。但由于依赖仿真数据，其在真实复杂人类行为下的表现仍待验证。

### 6. 主要结论与发现
*   **相关性泛化有效**：GP 网关能通过核函数将对某一工具的信任推广到相似工具上（如批准了“写入源码”会自动增加对“写入测试文件”的信任），准确率达 97.3%，远超独立学习基准。
*   **显著减轻负担**：相比于全人工审批，该方法将人工干预频率降低了约 **1.8 倍**，同时保持了极低的误报率（安全风险）。
*   **信任追踪**：模型能准确追踪人类信任的平滑漂移和突发重置（Changepoint）。
*   **诚实的负面结果**：实验发现，简单的“询问不确定区域”在样本效率上并未显著优于随机查询，这指出了未来研究需要更先进的主动学习采集准则。

### 7. 优点
*   **理论严谨**：将定性的治理问题转化为成熟的贝叶斯优化和偏好学习问题。
*   **冷启动友好**：通过先验分布和结构化核，新工具可以继承相似工具的安全属性，避免了每个新动作都要询问人类。
*   **动态适应**：能够处理人类信任随时间变化的非平稳特性。

### 8. 不足与局限
*   **数据局限性**：缺乏真实世界的长期人类审批数据集，仿真环境可能简化了人类决策的复杂性。
*   **采集效率**：目前的“询问带”策略在类别不平衡情况下（如大部分动作都是安全的）样本效率提升不明显。
*   **应用限制**：高斯过程的计算复杂度随观测点增加呈立方增长，虽然在单项目规模可行，但在超大规模历史数据下需要引入稀疏近似技术。

（完）
