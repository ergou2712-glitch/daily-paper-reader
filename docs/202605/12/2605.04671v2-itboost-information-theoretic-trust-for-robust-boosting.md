---
title: "ITBoost: Information-Theoretic Trust for Robust Boosting"
title_zh: ITBoost：面向鲁棒提升的信息论信任
authors: "Ye Su, Longlong Zhao, Diego Garcia-Gil, Jipeng Guo, Gangchun Zhang, Jinxin Chen, Jinsong Chen"
date: 2026-05-06
pdf: "https://arxiv.org/pdf/2605.04671v2"
tags: ["query:q5"]
score: 9.0
evidence: 使用最小描述长度原则测量残差轨迹的复杂性和信任度
tldr: 针对梯度提升算法在处理带噪声标签的表格数据时性能下降的问题，本文提出了 ITBoost。该方法不再仅依赖瞬时误差，而是通过最小描述长度（MDL）原则分析样本残差在迭代过程中的演化轨迹。通过识别并降低具有不规则残差波动样本的权重，ITBoost 显著提升了模型的鲁棒性。理论证明其具有更紧的泛化界，实验表明其在噪声环境下优于现有提升算法和深度学习模型，且在干净数据上保持领先。
selection_source: fresh_fetch
motivation: 传统的梯度提升算法容易过度关注高梯度噪声样本，导致在标签含有噪声时模型性能显著下降。
method: 提出 ITBoost 框架，利用最小描述长度原则评估样本残差轨迹的复杂性，对表现不规则的不可信样本进行降权处理。
result: 在多个表格数据集上的实验证明，ITBoost 在噪声环境下表现出更强的鲁棒性，同时在干净数据上保持了最优的平均性能。
conclusion: 通过信息论视角重新评估样本可靠性，ITBoost 为构建鲁棒的梯度提升模型提供了一种有效且具有理论支撑的新途径。
---

## 摘要
梯度提升仍然是表格数据学习中一种强大且广泛使用的方法，但当训练标签存在噪声时，其性能往往会下降。这种现象在很大程度上与提升算法强调大梯度样本的方式有关，而没有明确区分这些误差是源于包含信息的困难样本还是源于不可靠的标签。我们通过重新思考提升过程中样本可靠性的评估方式来解决这一问题。我们不再依赖瞬时误差，而是检查每个样本残差在迭代过程中的演变。基于这一见解，我们提出了信息论信任提升（ITBoost），它利用最小描述长度原则来衡量残差轨迹的复杂度。残差模式以不规则方式波动的样本被视为不可信，并在学习过程中被降低权重。在理论上，我们推导了 ITBoost 在标签噪声下的更紧致的泛化界。在各种表格基准测试上的实验结果表明，与领先的提升模型和深度表格模型相比，ITBoost 在噪声环境下提供了更好的鲁棒性，同时在干净数据上保持了最佳的平均性能。

## Abstract
Gradient boosting remains a strong and widely used method for tabular data learning, but its performance often degrades when training labels are noisy. This behavior is largely related to the way boosting algorithms emphasize samples with large gradients, without explicitly accounting for whether such errors originate from informative hard cases or from unreliable labels. We address this issue by reconsidering how sample reliability is evaluated during boosting. Instead of relying on instantaneous error, we examine the evolution of each sample's residuals across iterations. Based on this insight, we propose Information-Theoretic Trust Boosting (ITBoost), which uses the Minimum Description Length principle to measure the complexity of residual trajectories. Samples whose residual patterns fluctuate in an irregular manner are treated as less trustworthy and are down-weighted during learning. Theoretically, we derive a tighter generalization bound for ITBoost under label noise. Empirical results on various tabular benchmarks indicate that ITBoost provides improved robustness in noisy environments over leading boosting and deep tabular models, while retaining best average performance on clean data.

---

## 论文详细总结（自动生成）

这是一份关于论文《ITBoost: Information-Theoretic Trust for Robust Boosting》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
梯度提升决策树（GBDT）是处理表格数据的标准方法，但其核心机制存在一个固有缺陷：**对标签噪声极度敏感**。GBDT 通过拟合残差来迭代优化，这导致模型会过度关注梯度较大的样本。然而，算法无法区分大梯度是源于“靠近决策边界的困难样本”（有益信息）还是“标签错误的噪声样本”（有害信息）。这种盲目关注会导致模型在噪声环境下过度拟合，决策边界破碎，泛化性能剧降。

### 2. 论文提出的方法论
论文提出了 **ITBoost（信息论信任提升）**，其核心思想是：**利用样本残差的历史演化模式来评估其可信度，而非仅看瞬时误差大小。**

*   **核心逻辑**：
    *   **困难但干净的样本**：其残差序列通常具有结构化模式（如在边界附近震荡或单调减小），具有较低的算法复杂度。
    *   **噪声样本**：由于其标签与特征结构冲突，模型拟合时会产生随机、无序的残差波动，具有较高的算法复杂度。
*   **关键技术细节**：
    1.  **残差二值化**：在每轮迭代中，仅记录残差的符号（正/负），丢弃不稳定的幅值信息，形成每个样本的符号序列。
    2.  **复杂度度量**：采用信息论中的**最小描述长度（MDL）原则**，通过 **Lempel-Ziv (LZ) 算法**在线计算每个样本残差序列的复杂度 $C_i$。
    3.  **信任权重计算**：
        *   对复杂度进行归一化处理得到 $\bar{C}_i$。
        *   计算信任得分 $\tau_i = \exp(-\bar{C}_i)$。
        *   最终样本权重 $w_i = |g_i| \cdot \tau_i$，即用信任得分对梯度幅值进行指数级惩罚。
*   **理论支撑**：论文推导了 ITBoost 在对称标签噪声下的泛化界，证明了复杂度分离（Complexity Separation）能带来更紧致的误差界。

### 3. 实验设计
*   **数据集**：使用了 5 个公开的表格数据集：Madelon（高维合成）、Jasmine、Bioresponse、Creditcard（欺诈检测）和 OVA Uterus。
*   **噪声场景**：
    *   **对称标签噪声**（随机翻转标签）。
    *   **非对称标签噪声**（特定类别间的翻转）。
    *   **特征噪声**（在特征中加入随机干扰）。
*   **Benchmark（对比方法）**：
    *   **传统提升算法**：AdaBoost, GBDT, XGBoost, LightGBM, CatBoost, NGBoost。
    *   **深度学习模型**：TabPFN（基于 Transformer 的表格基础模型）。

### 4. 资源与算力
*   **硬件环境**：实验在 Windows 10 环境下完成，使用 Intel 处理器（64位，20个逻辑核心）。
*   **算力说明**：文中**未提及使用 GPU**，所有算法（包括 ITBoost 和 TabPFN）均在 CPU 环境下运行。
*   **时间开销**：ITBoost 引入了 LZ 复杂度计算，其时间复杂度为 $O(N \cdot M^2)$（$N$ 为样本数，$M$ 为迭代次数）。实验显示，ITBoost 的训练时间显著高于 LightGBM 等优化过的框架（例如在 Jasmine 数据集上，ITBoost 耗时 14.26s，而 LightGBM 仅需 0.22s）。

### 5. 实验数量与充分性
*   **实验规模**：涵盖了 5 个不同领域的数据集，每项实验均采用 **5 折分层交叉验证**。
*   **充分性**：
    *   **多维度对比**：不仅对比了准确率（ACC），还包括 F1、AUC 和 Log Loss。
    *   **消融实验**：对比了“残差二值化”与“多值量化”的效果，证明了二值化在过滤噪声方面的优越性。
    *   **可视化分析**：展示了易学样本、困难样本和噪声样本在训练过程中权重的演化轨迹，直观验证了方法的有效性。
    *   **统计检验**：使用了 **Friedman Test** 对所有算法进行排名和显著性差异分析，结果具有统计学意义。

### 6. 主要结论与发现
*   **鲁棒性极强**：在 10% 到 50% 的高噪声水平下，ITBoost 的性能下降速度远慢于 XGBoost、CatBoost 和 TabPFN。
*   **干净数据表现优异**：即使在没有人工加入噪声的原始数据集上，ITBoost 依然在多数指标上排名第一，说明其能自动识别并过滤数据中固有的隐性噪声。
*   **权重演化规律**：实验证实，噪声样本的权重在训练初期较高，但随着迭代增加，其残差复杂度迅速上升，权重随之发生“断崖式”下降。

### 7. 优点
*   **视角创新**：跳出了传统的“基于幅值”的鲁棒损失函数框架，从“时间序列复杂度”这一信息论视角解决噪声问题。
*   **无需先验**：不需要预知噪声比例或噪声分布，通过在线计算复杂度自动调整权重。
*   **理论与实践结合**：不仅有直观的算法设计，还提供了严谨的泛化误差界证明。

### 8. 不足与局限
*   **计算开销**：LZ 复杂度的实时计算和残差历史的存储带来了额外的计算和内存负担，在大规模数据集（如数百万行）上可能存在扩展性瓶颈。
*   **噪声类型限制**：该方法主要针对**随机/非结构化噪声**。如果噪声是系统性的或对抗性的（即噪声本身具有低复杂度模式），ITBoost 可能无法将其与困难样本区分开。
*   **超参数依赖**：虽然论文强调“开箱即用”，但归一化策略和指数惩罚函数的形式仍可能影响在特定任务上的表现。

（完）
