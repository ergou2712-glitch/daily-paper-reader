---
title: "From Questions to Trust Reports: A LLM-IR Framework for the TREC 2025 DRAGUN Track"
title_zh: 从问题到信任报告：面向 TREC 2025 DRAGUN 赛道的 LLM-IR 框架
authors: "Ignacy Alwasiak, Kene Nnolim, Jaclyn Thi, Samy Ateia, Markus Bink, Gregor Donabauer, David Elsweiler, Udo Kruschwitz"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23125v1"
tags: ["query:q3"]
score: 8.0
evidence: 用于检索增强可信度报告和评估在线新闻的框架
tldr: 针对TREC 2025 DRAGUN赛道，本文提出了UR_Trecking系统，旨在辅助用户评估在线新闻的可信度。该系统结合大语言模型（LLM）与信息检索技术，通过生成关键问题、利用思维链（CoT）进行查询扩展、以及使用monoT5重排序和LLM证据合成，实现了从问题生成到可信度报告的自动化流程，显著提升了检索相关性和领域信任度。
selection_source: fresh_fetch
motivation: 随着在线新闻可信度评估需求的增长，需要有效的工具辅助用户识别虚假信息并生成结构化的可信度报告。
method: 采用LLM生成问题并结合语义过滤与聚类，利用CoT查询扩展和monoT5重排序检索证据，最后由LLM合成带引用的报告。
result: 官方评估显示，CoT查询扩展和重排序显著提高了检索的相关性和领域信任度，而问题生成质量表现稳健但仍有提升空间。
conclusion: 研究验证了LLM-IR框架在可信度评估中的潜力，并指出了未来在增强系统鲁棒性和评估深度方面的改进方向。
---

## 摘要
TREC 2025 的 DRAGUN 赛道旨在满足日益增长的需求，即开发有效的支持工具来帮助用户评估在线新闻的可信度。我们描述了为任务 1（关键问题生成）和任务 2（检索增强的可信度报告）提交的 UR_Trecking 系统。我们的方法结合了基于大语言模型（LLM）的问题生成、语义过滤、利用聚类实现的多样性增强，以及多种查询扩展策略（包括基于推理的思维链扩展），旨在从 MS MARCO V2.1 分段语料库中检索相关证据。检索到的文档使用 monoT5 模型进行重排序，并结合 LLM 相关性评判器和领域级可信度数据集进行过滤。对于任务 2，选定的证据由 LLM 合成为带有引用的简洁可信度报告。官方评估结果表明，与基准检索相比，思维链查询扩展和重排序显著提高了相关性和领域信任度，而问题生成的性能表现中等，仍有改进空间。最后，我们概述了遇到的主要挑战，并为系统未来迭代中增强鲁棒性和可信度评估提出了建议方向。

## Abstract
The DRAGUN Track at TREC 2025 targets the growing need for effective support tools that help users evaluate the trustworthiness of online news. We describe the UR_Trecking system submitted for both Task 1 (critical question generation) and Task 2 (retrieval-augmented trustworthiness reporting). Our approach combines LLM-based question generation with semantic filtering, diversity enforcement using clustering, and several query expansion strategies (including reasoning-based Chain-of-Thought expansion) to retrieve relevant evidence from the MS MARCO V2.1 segmented corpus. Retrieved documents are re-ranked using a monoT5 model and filtered using an LLM relevance judge together with a domain-level trustworthiness dataset. For Task 2, selected evidence is synthesized by an LLM into concise trustworthiness reports with citations. Results from the official evaluation indicate that Chain-of-Thought query expansion and re-ranking substantially improve both relevance and domain trust compared to baseline retrieval, while question-generation performance shows moderate quality with room for improvement. We conclude by outlining key challenges encountered and suggesting directions for enhancing robustness and trustworthiness assessment in future iterations of the system.

---

## 论文详细总结（自动生成）

### 论文结构化总结：From Questions to Trust Reports

#### 1. 论文的核心问题与整体含义
本文针对在线新闻环境下虚假信息泛滥、用户评估能力不足（如过度自信或搜索偏见）的问题，提出了一个名为 **UR_Trecking** 的 LLM-IR（大语言模型与信息检索结合）框架。该研究是为 **TREC 2025 DRAGUN 赛道**设计的，旨在通过自动化手段生成引导用户批判性思考的“关键问题”（任务 1），并基于检索到的证据生成结构化的“可信度报告”（任务 2），从而辅助用户识别新闻的真实性与潜在偏见。

#### 2. 论文提出的方法论
该系统构建了一个端到端的流水线，核心步骤如下：
*   **问题生成与过滤（Task 1）**：
    *   **生成**：利用 GPT-4o-Nano 模型，根据预设的逻辑原则（如调查来源背景、所有权、编辑政策等）为每篇新闻生成约 30 个问题。
    *   **过滤与去重**：通过语义过滤剔除复合句和过长问题；利用 Sentence Transformer 将问题向量化，通过 **K-Means 聚类**选取每个簇中靠近质心的代表性问题，以确保问题的多样性。
*   **检索与报告生成（Task 2）**：
    *   **查询扩展**：对比了四种策略：基准（原问题）、布尔扩展（关键词组合）、**思维链（CoT）扩展**（引导 LLM 推理所需信息后生成查询）和结构化 DSL 扩展。
    *   **重排序与过滤**：使用 **monoT5** 模型对初筛的 1000 个文档进行重排序；引入 **Lin et al. 领域信任数据集**，根据 URL 域名分配信任分，并结合 LLM 作为相关性评判器（LLM Judge）进行双重过滤。
    *   **合成**：LLM 综合 Top-N 相关且高信任度的证据，生成带有引用的简洁可信度报告。

#### 3. 实验设计
*   **数据集**：使用 **MS MARCO V2.1 (Segmented)** 作为检索语料库。
*   **Benchmark**：TREC 2025 DRAGUN 官方评估体系，包括 `qgen_score`（问题生成得分）、`contradictory_score`（矛盾得分）和 `supportive_score`（支持性得分）。
*   **对比方法**：在检索阶段，重点对比了四种查询扩展策略在重排序前后的表现，评估指标涵盖相关性得分和领域信任得分。

#### 4. 资源与算力
*   **模型使用**：主要依赖 OpenAI 的 **GPT-4o-Nano** API 进行生成和评判；重排序使用了预训练的 `monot5-base-msmarco`。
*   **算力细节**：论文中**未明确说明**具体的 GPU 型号、数量或训练时长。由于该框架主要基于现有的 LLM API 和预训练模型推理，其主要的计算开销集中在 API 调用和本地重排序模型的推理上。

#### 5. 实验数量与充分性
*   **实验规模**：实验覆盖了官方提供的 30 个左右的主题（Topic）。
*   **消融与对比**：针对检索环节，对 4 种扩展策略在重排序前后的效果进行了详细的对比分析（如图 2-5 所示）。
*   **充分性评价**：检索部分的实验较为充分，展示了不同策略对结果质量的影响。然而，在问题生成（Task 1）方面，由于时间限制，作者仅提交了一个 Run，且未能集成计划中的“迭代修正循环”，这使得该部分的实验探索略显不足。

#### 6. 主要结论与发现
*   **CoT 扩展最优**：在所有查询扩展策略中，基于思维链（CoT）的扩展方法在相关性得分上表现最稳健，且方差最小。
*   **重排序的增益**：monoT5 重排序不仅显著提升了文档的相关性，还通过筛选机制间接提升了检索结果的领域信任度。
*   **生成质量的局限**：系统在避免生成“矛盾性”内容方面表现出色，但在“支持性”得分上较低，说明生成的报告与人类专家评估的契合度仍有提升空间。

#### 7. 优点
*   **多样性处理**：通过 K-Means 聚类解决 LLM 生成问题时的冗余性，确保了批判性视角的覆盖面。
*   **信任信号集成**：将外部的域名信任评分数据集引入 RAG 流程，为可信度评估提供了除语义相关性之外的客观维度。
*   **推理驱动检索**：验证了 CoT 在复杂信息检索任务（如新闻核查）中的有效性。

#### 8. 不足与局限
*   **系统完整性受限**：由于开发周期限制，缺失了关键的迭代修正环节，导致最终生成的问题和报告在官方评估中仅获得中等评分。
*   **LLM 评判风险**：内部评估高度依赖 LLM Judge，可能存在与人类真实判断不一致的校准风险。
*   **领域覆盖**：对于未包含在 Lin et al. 数据集中的域名，系统默认分配 0 分，这可能导致一些新兴或小众但可靠的媒体被误杀。

（完）
