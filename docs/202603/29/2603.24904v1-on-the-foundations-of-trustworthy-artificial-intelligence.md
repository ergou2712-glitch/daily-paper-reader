---
title: On the Foundations of Trustworthy Artificial Intelligence
title_zh: 论可信人工智能的基础
authors: TJ Dunham
date: 2026-03-26
pdf: "https://arxiv.org/pdf/2603.24904v1"
tags: ["query:q4"]
score: 9.0
evidence: 引入信任熵来量化非确定性的成本
tldr: 本研究探讨了可信人工智能的基础，提出“确定性论题”，认为平台确定性推理是实现AI可信的充要条件。研究引入了信任熵来量化非确定性成本，并证明了确定性对于高效验证的必要性。为解决浮点运算导致的非确定性，作者开发了一个纯整数推理引擎，在跨架构（ARM/x86）测试中实现了位级一致的输出，并通过链上存证验证了其在分布式环境下的可靠性，证明了AI可信性本质上是一个算术问题。
selection_source: fresh_fetch
motivation: 现有的AI推理由于硬件和浮点运算的非确定性，导致验证成本极高且难以实现真正的系统可信。
method: 提出确定性论题并引入信任熵，开发了一套基于Rust语言的纯整数推理引擎以消除跨平台的算术差异。
result: 在不同架构和地理位置的节点上实现了6.7B参数模型的位级一致性输出，并通过356次链上交易完成了验证。
conclusion: 平台确定性是实现AI可信属性的前提，AI的可信性最终取决于底层算术的一致性。
---

## 摘要
我们证明了平台确定性推理（platform-deterministic inference）是实现可信人工智能的充分必要条件。我们将其形式化为“确定性命题”（Determinism Thesis），并引入“信任熵”（trust entropy）来量化非确定性的成本，证明了验证失败概率精确等于 1 - 2^{-H_T}。我们证明了“确定性-验证坍缩”（Determinism-Verification Collapse）：在确定性条件下，验证仅需 O(1) 的哈希比较；若无此条件，验证者将面临一个难解的成员资格问题（membership problem）。IEEE 754 浮点运算从根本上违反了确定性要求。我们通过构建一个纯整数推理引擎解决了这一问题，该引擎在 ARM 和 x86 架构上实现了位级一致（bitwise identical）的输出。在针对参数量高达 67 亿（6.7B）模型的 82 次跨架构测试中，我们观察到哈希不匹配次数为零。四个地理分布的节点产生了完全相同的输出，并通过 356 笔链上证明交易进行了验证。人工智能系统的每一项主要信任属性（公平性、鲁棒性、隐私性、安全性、对齐）都以平台确定性为前提。我们的系统由部署在三大洲的 9.9 万行 Rust 代码组成，它确立了人工智能的信任本质上是一个算术问题。

## Abstract
We prove that platform-deterministic inference is necessary and sufficient for   trustworthy AI. We formalize this as the Determinism Thesis and introduce trust   entropy to quantify the cost of non-determinism, proving that verification failure   probability equals 1 - 2^{-H_T} exactly. We prove a Determinism-Verification   Collapse: verification under determinism requires O(1) hash comparison; without it,   the verifier faces an intractable membership problem. IEEE 754 floating-point   arithmetic fundamentally violates the determinism requirement. We resolve this by   constructing a pure integer inference engine that achieves bitwise identical output   across ARM and x86. In 82 cross-architecture tests on models up to 6.7B parameters,   we observe zero hash mismatches. Four geographically distributed nodes produce   identical outputs, verified by 356 on-chain attestation transactions. Every major   trust property of AI systems (fairness, robustness, privacy, safety, alignment)   presupposes platform determinism. Our system, 99,000 lines of Rust deployed across   three continents, establishes that AI trust is a question of arithmetic.

---

## 论文详细总结（自动生成）

这是一份关于论文《On the Foundations of Trustworthy Artificial Intelligence》（论可信人工智能的基础）的结构化分析报告：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：现代 AI 推理在不同硬件平台上具有**非确定性**。由于 IEEE 754 浮点运算不满足结合律（$(a+b)+c \neq a+(b+c)$），相同的模型和输入在不同处理器（如 ARM 与 x86）上会产生微小的舍入差异。
*   **研究背景**：这些微小的位级差异在深度神经网络（如 Transformer）中会随层数指数级放大，导致最终输出的 Token 序列完全不同。
*   **整体含义**：作者认为，如果 AI 输出不可预测且不可复现，那么现有的验证、审计、合规和安全认证框架都将失效。论文旨在证明“确定性”是可信 AI 的物理与数学基石。

### 2. 方法论：核心思想与关键技术
*   **确定性论题（Determinism Thesis）**：形式化证明了平台确定性是实现 AI 系统可验证性、可复现性、可审计性和可认证性的**充分必要条件**。
*   **信任熵（Trust Entropy, $H_T$）**：引入了基于 Rényi 碰撞熵的度量指标，证明验证失败的概率精确等于 $1 - 2^{-H_T}$。当且仅当 $H_T=0$ 时，验证才是完备的。
*   **确定性-验证坍缩**：证明了在确定性条件下，验证复杂度坍缩为 $O(1)$ 的哈希比较；而在非确定性下，验证变成了不可解的成员资格问题。
*   **ARC 推理引擎**：
    *   **纯整数算术**：完全弃用浮点运算，使用 INT8 权重和 Q16 定点数激活值。
    *   **整数算子重构**：实现了整数版的 RMSNorm（基于牛顿迭代法）、RoPE（旋转位置嵌入）和 SiLU 激活函数。
    *   **位级一致性**：利用整数运算在所有二补码架构上的代数结合律，确保跨平台输出绝对一致。
*   **信任栈构建**：结合 DAG 共识算法进行多节点输出对齐，并利用 Circle STARK 证明（Mersenne-31 域）实现计算完整性的简洁验证。

### 3. 实验设计
*   **数据集与模型**：使用了 Llama-2-7B（67 亿参数）和 TinyLlama-1.1B 模型。测试涵盖了数学逻辑、事实问答、代码生成、创意写作等场景。
*   **Benchmark 与对比**：
    *   **跨架构对比**：在 ARM (Apple M2 Ultra) 和 x86 (Vultr 云服务器) 之间进行位级一致性测试。
    *   **基准对比**：对比了基于浮点运算的 `candle` 推理后端。
    *   **质量评估**：通过 WikiText-2 数据集测量困惑度（Perplexity），评估整数化后的模型精度损失。
*   **分布式验证**：在分布于三大洲（洛杉矶、阿姆斯特丹、伦敦、新加坡）的 4 个节点上运行共识测试。

### 4. 资源与算力
*   **硬件设备**：
    *   **ARM 平台**：Apple M2 Ultra（24 核 CPU，76 核 GPU，64GB 统一内存）。
    *   **x86 平台**：Vultr 云实例（2 vCPU，8GB RAM）。
*   **算力消耗**：论文未明确给出总训练/运行小时数，但提供了推理延迟数据：在 M2 Ultra 上，7B 模型的推理速度为 76ms/token（GPU）和 139ms/token（CPU）。
*   **软件规模**：项目包含 99,000 行 Rust 代码和 1,231 个测试用例。

### 5. 实验数量与充分性
*   **实验规模**：
    *   **82 次跨架构测试**：涵盖不同模型和 Token 长度（最高 1024 tokens），实现 0 哈希失配。
    *   **30 组多节点共识实验**：验证了地理分布节点下的输出一致性。
    *   **60 个 STARK 证明生成**：覆盖了从 1B 到 70B 参数规模的层维度。
    *   **356 笔链上交易**：在测试网上记录了真实的推理存证。
*   **充分性评价**：实验设计非常客观且具有针对性。通过“浮点数必然发散”与“整数必然一致”的对照实验，有力地支撑了其理论推导。

### 6. 主要结论与发现
*   **算术决定信任**：AI 的信任问题不仅是算法对齐问题，更是底层的算术一致性问题。
*   **性能增益**：出乎意料的是，纯整数推理引擎在相同硬件上比优化的浮点后端快 **1.26x 到 2.3x**，因为它规避了浮点数的反常值处理和舍入管理开销。
*   **验证简化**：确定性使得昂贵的零知识证明（ZKP）可以与简单的重执行（Re-execution）结合，大幅降低了去中心化 AI 网络的验证成本。

### 7. 优点：亮点与创新
*   **理论深度**：首次将 AI 可信性与算术结合律联系起来，并给出了严谨的数学证明。
*   **工程完备性**：不仅停留在理论，还开发了高性能的 Rust 引擎，并在真实地理分布的区块链网络上进行了验证。
*   **硬件无关性**：证明了通过整数化，可以在异构硬件（CPU、GPU、甚至未来的加速器）之间建立统一的信任标准。

### 8. 不足与局限
*   **精度权衡**：INT8 量化虽然实现了确定性，但相比 FP16 原始模型存在精度损失（困惑度从 5.47 升至 144，尽管部分原因是 Chat 版与 Base 版的差异）。
*   **STARK 覆盖范围**：目前的零知识证明主要覆盖全连接层（Dense layers），尚未完全覆盖 Attention 和所有非线性激活函数。
*   **训练阶段缺失**：研究主要集中在推理阶段，尚未解决训练过程中的非确定性（如随机梯度下降、数据洗牌等）。
*   **应用限制**：对于极度依赖高浮点精度的科学计算类 AI 模型，强制整数化可能会带来较大的性能或效果挑战。

（完）
