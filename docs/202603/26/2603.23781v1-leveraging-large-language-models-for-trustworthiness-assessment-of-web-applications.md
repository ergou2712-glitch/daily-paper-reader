---
title: Leveraging Large Language Models for Trustworthiness Assessment of Web Applications
title_zh: 利用大语言模型进行 Web 应用程序的可信度评估
authors: "Oleksandr Yarotskyi, José D'Abruzzo Pereira, João R. Campos"
date: 2026-03-24
pdf: "https://arxiv.org/pdf/2603.23781v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 评估Web应用程序是否可信的系统方法
tldr: 本研究针对Web应用安全性评估中手动审查效率低、难以扩展的问题，提出了一种利用大语言模型（LLM）自动化验证安全编码实践的方法。通过对比五种主流LLM在不同提示工程技术下的表现，研究发现规则化指令提示能显著提升评估可靠性。此外，研究还扩展了基于逻辑偏好得分（LSP）的层次化质量模型，将LLM输出转化为综合信任分数，为实现可扩展且感知上下文的Web应用信任评估提供了可行方案。
selection_source: fresh_fetch
motivation: 现有的Web应用信任评估主要依赖手动代码审查，存在耗时、主观且难以大规模扩展的局限性。
method: 利用LLM验证安全编码实践，并结合提示工程与基于逻辑偏好得分（LSP）的层次化质量模型来计算综合信任分数。
result: 实验表明规则化指令提示可提高评估可靠性，而过多的结构化上下文（如调用图）可能引入噪声并干扰判断。
conclusion: 该方法能有效区分安全与脆弱的系统实现，证明了利用LLM进行自动化、可扩展的Web应用信任评估具有高度可行性。
---

## 摘要
Web 应用程序的广泛采用使其安全性成为一个关键关注点，并增加了对评估其是否可信的系统化方法的需求。然而，“信任”评估仍然是一个开放性问题，因为现有技术主要侧重于检测已知漏洞或依赖人工评估，这限制了其可扩展性；因此，通过关注可观察的开发行为来评估对安全编码实践的遵循情况，提供了一个互补且务实的视角。在实践中，安全编码实践的识别和验证主要由人工完成，依赖于专家知识和代码审查，这既耗时、主观，又难以扩展。本研究提出了一种实证方法，通过利用大语言模型（LLMs）验证对安全编码实践的遵循情况，从而实现 Web 应用程序可信度评估的自动化。我们对五种最先进的 LLM 进行了提示工程技术的对比分析，范围从基准零样本分类到富含语义定义、源自调用图的结构上下文以及显式指令引导的提示。此外，我们提出了一种基于逻辑偏好得分（LSP）的分层质量模型（QM）扩展，其中 LLM 的输出被用于填充模型的质量属性并计算整体可信度得分。实验结果表明，过多的结构上下文可能会引入噪声，而基于规则的指令提示则能提高评估的可靠性。由此产生的可信度得分能够区分安全实现与脆弱实现，支持了使用 LLM 进行可扩展且上下文感知的信任评估的可行性。

## Abstract
The widespread adoption of web applications has made their security a critical concern and has increased the need for systematic ways to assess whether they can be considered trustworthy. However, "trust" assessment remains an open problem as existing techniques primarily focus on detecting known vulnerabilities or depend on manual evaluation, which limits their scalability; therefore, evaluating adherence to secure coding practices offers a complementary, pragmatic perspective by focusing on observable development behaviors. In practice, the identification and verification of secure coding practices are predominantly performed manually, relying on expert knowledge and code reviews, which is time-consuming, subjective, and difficult to scale. This study presents an empirical methodology to automate the trustworthiness assessment of web applications by leveraging Large Language Models (LLMs) to verify adherence to secure coding practices. We conduct a comparative analysis of prompt engineering techniques across five state-of-the-art LLMs, ranging from baseline zero-shot classification to prompts enriched with semantic definitions, structural context derived from call graphs, and explicit instructional guidance. Furthermore, we propose an extension of a hierarchical Quality Model (QM) based on the Logic Score of Preference (LSP), in which LLM outputs are used to populate the model's quality attributes and compute a holistic trustworthiness score. Experimental results indicate that excessive structural context can introduce noise, whereas rule-based instructional prompting improves assessment reliability. The resulting trustworthiness score allows discriminating between secure and vulnerable implementations, supporting the feasibility of using LLMs for scalable and context-aware trust assessment.

---

## 论文详细总结（自动生成）

### 论文结构化深入总结：利用大语言模型进行 Web 应用程序的可信度评估

#### 1. 核心问题与整体含义（研究动机和背景）
随着 Web 应用程序的普及，其安全性已成为关键问题。传统的安全评估方法（如 SAST 和 DAST）存在局限性：SAST 误报率高且缺乏上下文，DAST 覆盖范围有限。此外，现有的“可信度”评估高度依赖安全专家的手动代码审查，这在敏捷开发和大规模系统中难以扩展。
本论文的核心问题是：**如何利用大语言模型（LLM）的语义理解能力，自动化地验证代码对安全编码实践（如 OWASP 指南）的遵循情况，并以此量化 Web 应用的可信度。** 研究旨在通过自动化手段替代或辅助人工审查，实现可扩展且上下文感知的安全评估。

#### 2. 论文提出的方法论
论文提出了一种结合 LLM 推理与形式化质量模型（QM）的系统化方法，主要包含以下核心环节：
*   **核心思想**：将安全评估转化为一个分类任务（LLM 判断代码是否遵循特定实践）和一个聚合任务（通过逻辑模型计算总分）。
*   **提示工程策略（Prompt Engineering）**：
    *   **P1 (Baseline)**：零样本（Zero-shot），仅提供实践描述和代码。
    *   **P2 (CWE + Examples)**：引入语义增强，提供相关的 CWE 编号和正负代码示例。
    *   **P3 (Call Context)**：引入结构上下文，利用 Joern 工具提取函数调用图，将相关辅助函数的代码也加入提示。
    *   **P4 (Rule-Based)**：规则化指令，提供明确的“if-then”判定逻辑，减少模型的开放性解释。
*   **可信度聚合模型**：
    *   采用**逻辑偏好得分（LSP）**方法。
    *   **公式逻辑**：使用广义合取/析取（GCD）函数，特别是“弱同时性”算子（幂平均 $r = -1$）。
    *   **计算流程**：$Trustworthiness(f) = (\sum_{i=1}^{n} w_i \cdot r_i^{-1})^{-1}$。其中 $w_i$ 是基于真实漏洞数据（CVE 频率）推导出的权重，$r_i$ 是 LLM 对各项实践的判定值（0 或 1）。这种方法确保了如果关键安全实践缺失，总分会大幅下降（体现“木桶效应”）。

#### 3. 实验设计
*   **数据集**：使用 **WSVD-Bench**，包含 21 个 Java Web 服务、80 个操作及 158 个 SQL 注入漏洞。实验对比了安全版本（Vx0）和脆弱版本（VxA）。
*   **评估对象**：16 项 OWASP 输入验证（Input Validation）安全编码实践。
*   **对比模型**：选取了 5 种主流 LLM：GPT-3.5-turbo、GPT-4o-mini、GPT-4.1-mini、GPT-4.1、Gemini-2.5-flash（注：文中型号可能对应特定 API 版本）。
*   **Benchmark**：以安全专家的手动标注作为 Ground Truth（基准真值）。
*   **对比维度**：不同提示策略（P1-P4）对模型分类准确率的影响，以及 LLM 直接打分与 LSP 模型聚合打分的可靠性对比。

#### 4. 资源与算力
*   **算力说明**：论文**未明确指出**具体的 GPU 型号、数量或训练时长。
*   **相关信息**：文中提到使用了 Google Cloud Research Credits 提供的计算资源，且所有 LLM 均通过 API 访问。由于该研究侧重于推理评估而非模型训练，因此算力消耗主要集中在 API 调用和静态分析工具（Joern）的运行上。

#### 5. 实验数量与充分性
*   **实验规模**：对 42 个 Java 函数进行了详尽测试，每个函数对应 16 项实践，跨越 5 个模型和 4 种提示策略，产生了数千次推理结果。
*   **充分性**：实验设计了消融对比（对比不同 Prompt 策略），并使用了 Mann-Whitney U 检验来验证统计显著性。
*   **客观性与公平性**：模型设置了统一的温度参数（Temperature = 0.2）以保证确定性；使用了宏观平均（Macro-averaging）指标来处理类别不平衡问题，实验设计较为严谨、客观。

#### 6. 论文的主要结论与发现
*   **模型表现分层**：GPT-4.1 在基准测试中表现最优（F1 ≈ 0.89），而 GPT-3.5 表现较差，容易产生幻觉。
*   **提示策略的影响**：
    *   **规则化指令（P4）最有效**：特别是对于轻量级模型（如 GPT-4o-mini），P4 显著提升了其可靠性。
    *   **结构上下文（P3）适得其反**：引入过多的辅助函数代码反而增加了噪声，导致模型召回率下降。
    *   **语义增强（P2）有两面性**：对高性能模型有帮助，但可能导致中等模型过度拟合示例模式。
*   **评分机制**：LLM 直接生成的信任分数不可靠（MAE 高达 0.4-0.6）；而通过 LLM 判定实践遵循情况、再由 LSP 模型聚合生成的得分，能有效区分安全与脆弱代码。

#### 7. 优点
*   **方法论结合严谨**：将 LLM 的语义推理能力与 LSP 这种成熟的多准则决策分析方法相结合，克服了 LLM 随机性导致的评分不稳。
*   **实用性强**：权重分配基于真实的 CVE 漏洞统计，使评估结果具有实证依据而非主观臆断。
*   **提示工程深入**：系统地探讨了上下文、示例和规则对安全审计任务的影响，为后续研究提供了参考。

#### 8. 不足与局限
*   **覆盖范围有限**：实验仅聚焦于 Java 语言和 SQL 注入相关的输入验证实践，未验证其在动态语言（如 Python、JS）或其他漏洞类型（如越权、加密失效）上的表现。
*   **闭源模型依赖**：研究完全依赖商业 API，未测试开源模型（如 Llama 3），存在成本和隐私合规的局限。
*   **上下文窗口限制**：P3 策略中 Gemini 模型因 Token 限制无法处理长上下文，说明该方法在处理超大规模复杂项目时仍面临技术瓶颈。
*   **基准真值主观性**：尽管由专家标注，但对“遵循实践”的定义在某些模糊场景下仍可能存在人为偏差。

（完）
