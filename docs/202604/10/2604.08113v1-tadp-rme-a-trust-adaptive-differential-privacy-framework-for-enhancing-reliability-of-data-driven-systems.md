---
title: "TADP-RME: A Trust-Adaptive Differential Privacy Framework for Enhancing Reliability of Data-Driven Systems"
title_zh: TADP-RME：一种用于增强数据驱动系统可靠性的信任自适应差分隐私框架
authors: "Labani Halder, Payel Sadhukhan, Sarbani Palit"
date: 2026-04-09
pdf: "https://arxiv.org/pdf/2604.08113v1"
tags: ["query:q4"]
score: 8.0
evidence: 利用逆信任评分自适应调节隐私预算
tldr: "针对数据驱动系统在对抗环境下的可靠性问题，本文提出TADP-RME框架。该框架通过引入逆信任评分动态调整差分隐私预算，解决了固定预算导致的效用与隐私权衡僵化问题。同时，利用逆流形嵌入技术对数据进行非线性变换，在保持差分隐私保证的同时破坏几何结构，有效抵御推理攻击。实验表明，该方法在不显著降低效用的情况下，将攻击成功率降低了3.1%，为对抗环境下的可靠学习提供了统一方案。"
selection_source: fresh_fetch
motivation: 现有的差分隐私方案因固定隐私预算和保留几何结构，难以应对异构用户信任及针对性的推理攻击。
method: 提出一种结合逆信任评分动态调节隐私预算与逆流形嵌入非线性变换的自适应差分隐私框架。
result: "理论与实验证明该框架在维持效用的同时，能将推理攻击的成功率最高降低3.1%，优于现有方法。"
conclusion: TADP-RME通过自适应调节和几何结构破坏，显著提升了数据驱动系统在复杂对抗环境下的可靠性与隐私保护能力。
---

## 摘要
在对抗性环境下确保可靠性，必须将隐私视为数据驱动系统的基础组成部分。虽然差分隐私和加密协议提供了强有力的保证，但现有方案依赖于固定的隐私预算，导致效用与隐私之间存在僵化的权衡，在用户信任度不均的情况下会失效。此外，仅添加噪声的差分隐私会保留几何结构，推理攻击可利用该结构导致隐私泄露。我们提出了 TADP-RME（基于逆流形嵌入的信任自适应差分隐私），这是一个能在不同用户信任水平下增强可靠性的框架。它引入了范围在 [0,1] 之间的逆信任评分来自适应地调节隐私预算，从而实现效用与隐私之间的平滑过渡。此外，逆流形嵌入通过非线性变换破坏局部几何关系，同时利用后处理特性保留形式化的差分隐私保证。理论和实证结果表明，该框架改进了隐私与效用之间的权衡，在不显著降低效用的情况下，将攻击成功率降低了高达 3.1%。该框架在对抗推理攻击方面始终优于现有方法，为对抗环境下的可靠学习提供了一种统一的方法。

## Abstract
Ensuring reliability in adversarial settings necessitates treating privacy as a foundational component of data-driven systems. While differential privacy and cryptographic protocols offer strong guarantees, existing schemes rely on a fixed privacy budget, leading to a rigid utility-privacy trade-off that fails under heterogeneous user trust. Moreover, noise-only differential privacy preserves geometric structure, which inference attacks exploit, causing privacy leakage.   We propose TADP-RME (Trust-Adaptive Differential Privacy with Reverse Manifold Embedding), a framework that enhances reliability under varying levels of user trust. It introduces an inverse trust score in the range [0,1] to adaptively modulate the privacy budget, enabling smooth transitions between utility and privacy. Additionally, Reverse Manifold Embedding applies a nonlinear transformation to disrupt local geometric relationships while preserving formal differential privacy guarantees through post-processing.   Theoretical and empirical results demonstrate improved privacy-utility trade-offs, reducing attack success rates by up to 3.1 percent without significant utility degradation. The framework consistently outperforms existing methods against inference attacks, providing a unified approach for reliable learning in adversarial environments.

---

## 论文详细总结（自动生成）

这是一份关于论文《TADP-RME: A Trust-Adaptive Differential Privacy Framework for Enhancing Reliability of Data-Driven Systems》的结构化深度总结：

### 1. 核心问题与整体含义（研究动机和背景）
*   **核心问题**：传统的差分隐私（DP）机制在实际应用中存在两个主要缺陷：
    1.  **僵化的隐私预算**：通常采用固定的隐私预算 $\epsilon$，无法适应不同用户、应用或环境下的异构信任需求。
    2.  **几何结构残留**：仅通过添加噪声（如高斯或拉普拉斯噪声）虽然能保护数值隐私，但往往保留了数据的局部几何结构（如邻域关系、聚类特征），攻击者可利用这些残余结构进行推理攻击（如成员推理、重构攻击）。
*   **整体含义**：论文将隐私泄露视为一种“系统故障”，从可靠性工程的角度出发，旨在构建一个既能根据信任度动态调整保护强度，又能通过破坏几何结构来增强抗攻击能力的统一框架。

### 2. 论文提出的方法论
该框架名为 **TADP-RME**，由两个核心组件构成：
*   **信任自适应差分隐私（TADP）**：
    *   引入逆信任评分 $T \in [0, 1]$，其中 $T=0$ 代表完全信任（高精度/低隐私），$T=1$ 代表完全不信任（低精度/高隐私）。
    *   **动态预算公式**：$\epsilon_T = \epsilon_{max} - T(\epsilon_{max} - \epsilon_{min})$。通过该公式，系统能根据环境风险平滑地调节噪声方差 $\sigma^2_T$。
*   **逆流形嵌入（RME）**：
    *   **核心思想**：在加噪后的数据上应用非线性周期性变换，将数据从 $d$ 维映射到 $2d$ 维。
    *   **技术细节**：变换公式为 $\phi(x_i) = (x_i \cos(\alpha x_i), x_i \sin(\alpha x_i))$。这种变换故意扭曲了原始空间的邻域关系（原本相近的点在变换后可能变远），增加了逆向重构的组合复杂度和歧义性。
    *   **理论保证**：基于 DP 的后处理性（Post-processing Property），这种确定性的非线性变换不会削弱已有的 $(\epsilon, \delta)$-DP 形式化保证。

### 3. 实验设计
*   **数据集**：使用了三个不同复杂度的基准数据集：**MNIST**（简单灰度）、**Fashion-MNIST**（中等复杂度）、**CIFAR-10**（高维自然图像）。
*   **对比方法（Baselines）**：
    *   **噪声类**：标准高斯 DP、拉普拉斯 DP、个性化 DP (PDP)。
    *   **投影/几何类**：随机投影 (Random Projection)、重构抗性隐私 (Reconstruction-Resistant)。
    *   **编码类**：局部敏感哈希 (LSH)、二进制编码 (Binary Encoding)。
    *   **基础类**：简单加性噪声。
*   **评估指标**：分类准确率（效用）、k-NN 重叠率（局部结构保存）、Spearman 相关系数（全局结构保存）、以及针对 MIA（成员推理）、AIA（属性推理）和重构攻击的隐私评分。

### 4. 资源与算力
*   **算力说明**：论文**未明确提及**具体的硬件配置（如 GPU 型号、显存大小）或具体的训练时长。
*   **实现细节**：提到使用 Python 语言，并依赖 Scikit-Learn 和 NumPy 库进行算法实现。由于主要涉及线性分类器（逻辑回归）和数学变换，推测其对算力的需求相对较低，普通的 CPU 或单卡 GPU 环境即可完成。

### 5. 实验数量与充分性
*   **实验规模**：
    *   在 3 个数据集上分别测试了 8 组不同的信任梯度（$\tau$ 值从 0 到 1）。
    *   每组实验均运行 5 次独立随机试验并取均值和标准差。
    *   进行了**消融实验**，分别验证了“仅加噪”、“仅嵌入”、“非自适应”与“全框架”的效果。
    *   进行了**参数敏感性分析**，探讨了 $\epsilon_{min}, \epsilon_{max}$ 和剪切范数 $C$ 的影响。
*   **充分性评价**：实验设计较为全面，涵盖了从效用到多种攻击模式的防御测试。通过配对 t 检验（p < 0.05）验证了结果的统计显著性，实验结果具有较高的客观性和说服力。

### 6. 主要结论与发现
*   **隐私-效用平衡优化**：TADP-RME 在保持与标准 DP 相当的效用时，通过 RME 变换将推理攻击的成功率降低了约 **1.1% 至 3.1%**。
*   **结构破坏有效性**：实验证明 RME 能显著降低 k-NN 重叠率和全局距离相关性，这直接导致了重构攻击难度的指数级增加。
*   **复杂度影响**：对于简单数据集（MNIST），框架能维持极高的效用；但对于高维复杂数据集（CIFAR-10），隐私保护会导致效用较快下降，显示出高维数据对扰动的敏感性。
*   **计算效率**：在高隐私级别下，由于数据结构被简化，系统的运行时间反而有所下降。

### 7. 优点
*   **灵活性**：引入信任评分 $T$，使隐私保护从“一刀切”转变为“按需分配”，更符合实际业务场景。
*   **多重防御**：结合了随机扰动（数值隐私）和几何扭曲（结构隐私），弥补了传统 DP 无法防御结构化推理攻击的短板。
*   **理论与实证结合**：既有严格的 DP 形式化证明，又有针对现代攻击手段（MIA, AIA）的实证评估。

### 8. 不足与局限
*   **高维效用损失**：在 CIFAR-10 等复杂数据集上，当隐私要求较高时，分类准确率下降剧烈（接近随机猜测），说明该方法在极高维特征空间的效用保持能力有限。
*   **隐私预算设置**：实验中使用的 $\epsilon$ 值（15 到 80）在理论 DP 研究中被认为偏高（通常 $\epsilon < 10$ 才被视为强隐私），这可能削弱了其在严苛隐私合规场景下的说服力。
*   **模型局限性**：实验主要基于线性探针（逻辑回归），未深入探讨该框架在深度神经网络（DNN）内部层应用时的梯度消失或训练稳定性问题。

（完）
