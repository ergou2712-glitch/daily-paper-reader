---
title: Convergence of Byzantine-Resilient Gradient Tracking via Probabilistic Edge Dropout
title_zh: 基于概率边丢弃的拜占庭容错梯度追踪的收敛性
authors: "Amirhossein Dezhboro, Fateme Maleki, Arman Adibi, Erfan Amini, Jose E. Ramirez-Marquez"
date: 2026-04-01
pdf: "https://arxiv.org/pdf/2604.00449v2"
tags: ["query:q4"]
score: 8.0
evidence: 针对对抗性通信的双指标信任评分
tldr: 本研究针对分布式网络中拜占庭节点的恶意攻击问题，提出了基于概率边缘丢弃的梯度追踪方法（GT-PD）。该方法通过自我中心投影限制消息范围，并利用双指标信任评分实现去中心化的概率丢弃，从而在抵御攻击的同时保留了双随机混合结构。针对持续性扰动，进一步引入漏积分器（GT-PD-L）以控制误差累积。理论证明了其线性收敛性，实验显示在多种攻击下性能显著优于传统鲁棒聚合算法。
selection_source: fresh_fetch
motivation: 旨在解决分布式优化中拜占庭节点通过发送虚假信息破坏梯度追踪算法收敛性和一致性的挑战。
method: 提出了结合自我中心投影剪裁和基于双指标信任评分的概率边缘丢弃机制的GT-PD算法及其漏积分改进版GT-PD-L。
result: 理论证明了算法在不同隔离程度下的线性收敛性，并在MNIST实验中展现出比坐标修剪均值法高出4.3个百分点的准确率。
conclusion: 通过概率边缘丢弃和漏积分技术，该研究为去中心化网络提供了一种在对抗环境下具备强鲁棒性和收敛保障的梯度追踪方案。
---

## 摘要
我们研究了包含拜占庭节点的网络上的分布式优化问题，这些节点可能会发送任意的对抗性消息。我们提出了“基于概率边丢弃的梯度追踪”（GT-PD），这是一种随机梯度追踪方法，能够在对抗性通信下保持梯度追踪的收敛特性。GT-PD 结合了两个互补的防御层：一种通用的以自我为中心的投影，将每个传入消息裁剪到接收节点周围半径为 $τ$ 的球体内；以及一种完全去中心化的概率丢弃规则，由决策和追踪通道中的双指标信任评分驱动。该设计在限制对抗性扰动的同时，保留了双随机混合结构，而这一性质在去中心化设置下的鲁棒聚合中通常会丢失。在完全拜占庭隔离（$p_b=0$）的情况下，GT-PD 线性收敛至一个仅由随机梯度方差决定的邻域。对于部分隔离（$p_b>0$）的情况，我们引入了“具有概率边丢弃和漏积分的梯度追踪”（GT-PD-L），它使用漏积分器来控制由持续扰动引起的追踪误差累积，并实现线性收敛至一个由随机方差和裁剪泄漏比决定的有界邻域。我们进一步证明，在 $p_h=1$ 的两层丢弃机制下，隔离拜占庭节点不会给诚实节点的共识动态引入额外的方差。在 MNIST 数据集上针对符号翻转（Sign Flip）、ALIE 和内积操纵（Inner Product Manipulation）攻击的实验表明，在隐蔽攻击下，GT-PD-L 的性能优于逐坐标修剪平均值多达 4.3 个百分点。

## Abstract
We study distributed optimization over networks with Byzantine agents that may send arbitrary adversarial messages. We propose \emph{Gradient Tracking with Probabilistic Edge Dropout} (GT-PD), a stochastic gradient tracking method that preserves the convergence properties of gradient tracking under adversarial communication. GT-PD combines two complementary defense layers: a universal self-centered projection that clips each incoming message to a ball of radius $τ$ around the receiving agent, and a fully decentralized probabilistic dropout rule driven by a dual-metric trust score in the decision and tracking channels. This design bounds adversarial perturbations while preserving the doubly stochastic mixing structure, a property often lost under robust aggregation in decentralized settings. Under complete Byzantine isolation ($p_b=0$), GT-PD converges linearly to a neighborhood determined solely by stochastic gradient variance. For partial isolation ($p_b>0$), we introduce \emph{Gradient Tracking with Probabilistic Edge Dropout and Leaky Integration} (GT-PD-L), which uses a leaky integrator to control the accumulation of tracking errors caused by persistent perturbations and achieves linear convergence to a bounded neighborhood determined by the stochastic variance and the clipping-to-leak ratio. We further show that under two-tier dropout with $p_h=1$, isolating Byzantine agents introduces no additional variance into the honest consensus dynamics. Experiments on MNIST under Sign Flip, ALIE, and Inner Product Manipulation attacks show that GT-PD-L outperforms coordinate-wise trimmed mean by up to 4.3 percentage points under stealth attacks.

---

## 论文详细总结（自动生成）

以下是对论文《Convergence of Byzantine-Resilient Gradient Tracking via Probabilistic Edge Dropout》（基于概率边丢弃的拜占庭容错梯度追踪的收敛性）的结构化深度总结：

### 1. 核心问题与研究动机
*   **核心问题**：在去中心化网络中，如何使“梯度追踪”（Gradient Tracking, GT）算法具备抵御“拜占庭节点”（即发送任意恶意消息的攻击者）的能力。
*   **研究动机**：
    *   **现有方案缺陷**：传统的鲁棒聚合规则（如中位数、修剪均值）在去中心化设置下会破坏混合矩阵的“双随机性”（Doubly Stochasticity），导致诚实节点间产生偏差和不一致。
    *   **攻击多样性**：拜占庭节点可以通过操纵参数空间或梯度追踪空间来破坏优化过程，现有的空间过滤方法难以同时兼顾收敛精度和拓扑稳定性。

### 2. 方法论
论文提出了 **GT-PD** 及其改进版 **GT-PD-L**，核心包含两个防御层：
*   **自我中心投影（Self-centered Projection）**：
    *   接收节点将所有传入消息裁剪到以自身状态为中心、半径为 $\tau$ 的 $\ell_2$ 球内。
    *   **作用**：在不改变混合矩阵结构的前提下，确定性地限制了单次迭代中对抗性扰动的幅度。
*   **概率边丢弃（Probabilistic Edge Dropout）**：
    *   **双指标信任评分**：基于空间通道（决策变量 $x$）和时间通道（梯度追踪变量 $y$）的偏差计算信任分。
    *   **核心公式**：保留概率 $p_{ij} = \exp(-\lambda \cdot S_{ij})$，其中 $S_{ij}$ 综合了两个通道的归一化偏差。若任一通道异常，保留概率趋于零。
    *   **结构优势**：通过将丢弃边的权重吸收进自环，确保了混合矩阵在任何实现下都保持双随机性。
*   **漏积分器（Leaky Integration, GT-PD-L）**：
    *   针对无法完全隔离拜占庭节点的情况（部分隔离），在梯度追踪更新中引入衰减因子 $\beta$。
    *   **作用**：防止对抗性扰动在追踪通道中无限累积，确保算法线性收敛至有界邻域。

### 3. 实验设计
*   **数据集与场景**：使用 **MNIST** 图像分类任务，采用非独立同分布（Non-IID）的数据划分（Dirichlet 分布）。
*   **网络设置**：20个节点，其中4个为拜占庭节点（20% 攻击者），采用随机正则图拓扑。
*   **攻击模型（Benchmark Attacks）**：
    1.  **Sign Flip（符号翻转）**：高强度方向性攻击。
    2.  **ALIE（A Little Is Enough）**：隐蔽攻击，将扰动控制在诚实梯度统计范围内。
    3.  **IPM（Inner Product Manipulation）**：诱导算法缓慢发散。
*   **对比方法**：无保护的梯度追踪（Unprotected GT）、坐标修剪均值法（Coordinate-wise Trimmed Mean, CWTM）。

### 4. 资源与算力
*   **算力说明**：论文**未明确说明**具体的 GPU 型号、数量或确切的训练时长。
*   **实验参数**：提到了 Batch Size 为 128，训练 30 个 Epoch，学习率 $\alpha=0.05$，正则化系数 $\mu=0.01$。

### 5. 实验数量与充分性
*   **实验规模**：针对三种主流攻击模型分别进行了对比实验，并提供了测试准确率、共识误差（$C_k$）和保留概率随时间变化的诊断曲线。
*   **充分性评价**：
    *   **优点**：实验覆盖了从显性到隐性的多种攻击手段，验证了“防御深度”特性（即当丢弃层失效时，投影层和漏积分层仍能维持收敛）。
    *   **局限**：仅使用了 MNIST 一个数据集，且网络规模（20节点）相对较小，未在大规模深度学习模型或更复杂的网络拓扑上进行验证。

### 6. 主要结论与发现
*   **线性收敛性**：理论证明了在完全隔离下可收敛至随机噪声水平；在部分隔离下，GT-PD-L 保持线性收敛。
*   **零惩罚隔离**：证明了在特定条件下，隔离拜占庭节点不会降低诚实节点间的共识速率。
*   **性能优势**：在 ALIE 隐蔽攻击下，GT-PD-L 的准确率比 CWTM 高出 **4.3 个百分点**。
*   **防御协同**：实验证明漏积分器是必不可少的，否则即使有投影保护，追踪误差的累积也会导致算法最终失效。

### 7. 优点与亮点
*   **数学严谨性**：通过 Lyapunov 函数分析，给出了明确的收敛界限和步长限制条件。
*   **结构保持**：巧妙地利用概率丢弃解决了去中心化鲁棒聚合中“双随机性丢失”的顽疾。
*   **双通道检测**：引入梯度追踪通道的信任评分，封堵了攻击者仅在单一维度伪装的漏洞。

### 8. 不足与局限
*   **超参数敏感性**：投影半径 $\tau$、保留概率敏感度 $\lambda$ 和漏系数 $\beta$ 需要精细调优，论文虽给出了理论指导，但实际应用中可能存在挑战。
*   **实验覆盖面**：缺乏在自然语言处理或大型视觉模型上的验证，且未讨论异步通信环境下的表现。
*   **计算开销**：虽然算法是去中心化的，但每个节点需要维护额外的信任评分和投影计算，在大规模稠密网络中可能增加计算负担。

（完）
