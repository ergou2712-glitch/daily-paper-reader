---
title: Trust-Aware Routing for Distributed Generative AI Inference at the Edge
title_zh: 边缘侧分布式生成式人工智能推理的信任感知路由
authors: "Chanh Nguyen, Erik Elmroth"
date: 2026-03-30
pdf: "https://arxiv.org/pdf/2603.28622v1"
tags: ["query:profile-1"]
score: 8.0
evidence: 考虑可靠性和节点信任的信任感知协作框架
tldr: 针对生成式AI在边缘设备分布式推理中面临的可靠性与信任挑战，本文提出G-TRAC框架。该框架将路由问题建模为风险受限最短路径，结合信任剪枝与Dijkstra算法实现低延迟路径选择，并采用混合信任架构维护全局声誉。实验证明，G-TRAC能有效隔离不可靠节点，显著提升推理完成率，确保动态环境下的鲁棒执行。
selection_source: fresh_fetch
motivation: 传统的尽力而为点对点路由无法应对边缘设备分布式推理中的单点故障、性能波动及节点误行为。
method: 提出G-TRAC框架，通过风险受限最短路径算法进行路径选择，并利用混合信任架构同步全局声誉状态。
result: 路由延迟在实际规模下低于1毫秒，且在节点故障和网络分区情况下仍能显著提高推理完成率并隔离不可靠节点。
conclusion: G-TRAC为去中心化边缘环境下的生成式AI推理提供了一种兼顾性能、可靠性与信任的鲁棒协调方案。
---

## 摘要
新兴的生成式人工智能部署越来越多地在去中心化且异构的边缘设备上执行推理，而非在单个受信任的服务器上。在这些环境中，单个设备的故障或异常行为可能会中断整个推理过程，使得传统的尽力而为（best-effort）点对点路由不再适用。因此，协调分布式生成式推理需要能够明确考虑参与节点之间的可靠性、性能波动以及信任度的机制。在本文中，我们提出了 G-TRAC，这是一个信任感知协调框架，它将算法路径选择与系统级协议设计相结合，以确保鲁棒的分布式推理。首先，我们将路由问题建模为“风险受限最短路径”（Risk-Bounded Shortest Path）计算，并引入了一种结合信任底线剪枝与 Dijkstra 搜索的多项式时间解法，在实际边缘规模下实现了亚毫秒级的路由延迟中位数，在大规模场景下延迟仍保持在 10 毫秒以下。其次，为了在动态环境中支持路由逻辑的运行，该框架采用了“混合信任架构”（Hybrid Trust Architecture），在稳定锚点维护全局信誉状态，同时通过后台同步向边缘节点分发轻量级更新。在商用设备组成的异构测试平台上的实验评估表明，G-TRAC 显著提高了推理完成率，有效隔离了不可靠节点，并且即使在节点故障和网络分区的情况下也能维持鲁棒执行。

## Abstract
Emerging deployments of Generative AI increasingly execute inference across decentralized and heterogeneous edge devices rather than on a single trusted server. In such environments, a single device failure or misbehavior can disrupt the entire inference process, making traditional best-effort peer-to-peer routing insufficient. Coordinating distributed generative inference therefore requires mechanisms that explicitly account for reliability, performance variability, and trust among participating peers.   In this paper, we present G-TRAC, a trust-aware coordination framework that integrates algorithmic path selection with system-level protocol design to ensure robust distributed inference. First, we formulate the routing problem as a \textit{Risk-Bounded Shortest Path} computation and introduce a polynomial-time solution that combines trust-floor pruning with Dijkstra's search, achieving sub-millisecond median routing latency at practical edge scales, and remaining below 10 ms at larger scales. Second, to operationally support the routing logic in dynamic environments, the framework employs a \textit{Hybrid Trust Architecture} that maintains global reputation state at stable anchors while disseminating lightweight updates to edge peers via background synchronization.   Experimental evaluation on a heterogeneous testbed of commodity devices demonstrates that G-TRAC significantly improves inference completion rates, effectively isolates unreliable peers, and sustains robust execution even under node failures and network partitions.

---

## 论文详细总结（自动生成）

以下是对论文《Trust-Aware Routing for Distributed Generative AI Inference at the Edge》的结构化总结：

### 1. 核心问题与整体含义
*   **研究背景**：生成式人工智能（GenAI）模型规模巨大，单一边缘设备（如手机、IoT网关）难以独立承载。现有的分布式推理方案（如流水线并行）多运行在受信任的数据中心，或在边缘侧采用“尽力而为”的路由策略。
*   **核心问题**：边缘环境具有高度的**异构性**和**不可靠性**。节点可能随时掉线、性能波动或产生误行为。传统的路由算法只关注延迟，容易选择“快但极不稳定”的节点，导致推理任务频繁中断。
*   **研究目的**：提出一种能在去中心化边缘网络中，平衡推理延迟、执行可靠性和节点信任度的协调框架。

### 2. 方法论：核心思想与关键技术
*   **核心思想**：将分布式推理路由建模为**风险受限最短路径（Risk-Bounded Shortest Path, RBSP）**问题，通过量化节点信任度来约束路径选择。
*   **关键技术细节**：
    *   **混合信任架构**：在稳定的基础设施（锚点 Anchor）维护全局信誉账本，边缘节点通过后台异步同步状态，避免控制面通信阻塞推理关键路径。
    *   **风险建模**：定义路径风险为节点可靠性的乘积。引入“有效延迟成本”公式，将节点预估延迟与失败惩罚（超时重路由时间）相结合。
    *   **信任底线剪枝（Trust-floor Pruning）**：为解决RBSP这一NP-Hard问题，G-TRAC先根据全局风险容忍度计算局部信任阈值 $\tau$，剔除不达标节点，将问题简化为在“信任子图”上进行多项式时间的 **Dijkstra 搜索**。
    *   **反馈驱动执行**：支持“单次修复”策略，即某跳失败时在本地尝试替换节点，并根据执行结果（成功/失败）向锚点反馈以更新节点信任分。

### 3. 实验设计
*   **实验场景**：校园网环境下的真实异构测试床，包含服务器、桌面电脑、笔记本和安卓手机。
*   **模型与负载**：使用 **GPT-2 Large** (774M参数)，将其层级切分并部署在不同节点上。
*   **对比方法 (Benchmarks)**：
    *   **Shortest Path (SP)**：传统最短路径，仅优化延迟。
    *   **Max-Reliability (MR)**：保守策略，仅优化可靠性。
    *   **LARAC**：基于拉格朗日松弛法的经典受限最短路径启发式算法。
    *   **Naive**：随机采样可行路径。
*   **节点画像**：人为设置了“蜜罐节点”（极快但极不可靠）、“乌龟节点”（极慢但极稳）等，以测试算法的辨别能力。

### 4. 资源与算力
*   **硬件设备**：
    *   4台 AMD Opteron 服务器 (47GB RAM)。
    *   4台 Intel Xeon 旧服务器 (15GB RAM)。
    *   1台 i5 桌面机及1台 i7 笔记本。
    *   2部 ARMv8 架构智能手机（通过 Termux 运行 Linux 环境）。
*   **网络环境**：基于 WireGuard/Tailscale 的加密网状覆盖网络。
*   **算力说明**：论文未提及大规模训练算力，重点在于**推理阶段**的资源消耗。实验显示，分布式部署使单设备内存需求从 4GB+ 降至 1GB 左右，CPU占用率从 900%+ 降至 10% 以下。

### 5. 实验数量与充分性
*   **实验规模**：
    *   针对不同生成长度（10、20、50 tokens）进行了多组对比实验。
    *   每组配置执行 **100次独立请求**，并计算 95% 置信区间。
    *   进行了扩展性测试，模拟网络规模从 50 扩展至 **1000 个节点**。
*   **充分性评价**：实验设计较为全面，涵盖了成功率、每 Token 延迟、路径跳数分布、节点选择偏好及算法计算开销等维度，客观地展示了算法在极端异构环境下的鲁棒性。

### 6. 主要结论与发现
*   **性能平衡**：G-TRAC 在成功率上接近 MR（最大可靠性），在延迟上接近 SP（最短路径），成功避开了“蜜罐节点”陷阱。
*   **可靠性**：在长文本生成（50 tokens）任务中，SP 算法成功率几乎归零，而 G-TRAC 仍能保持极高成功率。
*   **低开销**：G-TRAC 的路由决策延迟在千节点规模下仍低于 **10ms**（中位数亚毫秒级），远优于 LARAC 和 Naive 搜索。
*   **可行性**：证明了通过层级切分，在手机等受限设备上运行大模型推理是可行的，且能显著降低单机资源压力。

### 7. 优点
*   **算法高效性**：通过信任剪枝巧妙地将复杂约束优化问题转化为简单最短路径问题，极适合计算能力有限的边缘设备。
*   **架构合理性**：控制面与数据面解耦，异步同步机制保证了推理的实时性。
*   **实战导向**：在包含真实手机的异构网络中验证，比纯数学仿真更具说服力。

### 8. 不足与局限
*   **模型规模限制**：实验仅使用了 GPT-2 Large，未在百亿级（7B+）或混合专家模型（MoE）上验证，后者对带宽的要求更高。
*   **信任模型简化**：假设节点故障是相互独立的，未考虑大规模网络波动导致的关联性故障。
*   **安全深度不足**：目前的“信任”主要指“可靠性”，对于恶意篡改模型权重或窃取隐私数据的**拜占庭攻击**缺乏深入的防御机制探讨。

（完）
